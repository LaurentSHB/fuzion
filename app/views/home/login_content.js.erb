<% if user_signed_in? %>

  Bienvenue <%= current_user.full_name %>  |
  <%=  link_to  "Mon Profil", edit_user_path(current_user.id)   %> |
  <% if current_user.is_admin_or_super_admin? %>
    <%=  link_to  "Administration", admin_root_path   %> |
  <% end %>
  <%= link_to 'Se déconnecter', destroy_user_session_path, :method => :delete %>

<% else %>
  Bienvenue <%= link_to('Identifiez vous', new_user_session_path)  %> 
<% end %>